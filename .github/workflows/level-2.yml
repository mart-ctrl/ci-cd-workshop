#############################################
# Level 2 — Koopa's Fortress
#
# Doel:
#  Versla de Koopa‑bewakers met je test‑skills.
#
# Daadwerkelijk doel:
# - Tests uitvoeren in CI
# - Zorgen dat failures de pipeline laten falen
# - Test rapporten archiveren om te onderzoeken
#############################################
name: Level 2 - Koopa's Fortress

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v6

        # TODO - Enkel bovenstaande stap en de rest zelf laten uitvogelen en pull request weg

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run tests (Fire Flower)
        run: # TODO: Voer tests uit

      - name: Upload test reports
        if: # TODO: Wil je deze step altijd uitvoeren of bij een specifiek resultaat van de vorige step?
        uses: # TODO: Upload een artifact van bestanden uit de runner
        with:
          name: test-reports
          path: # |
          # Hint: Welke bestanden heb je na het uitvoeren van de testen (bij gebruik van Gradle)
          # TODO: Test Resultaten (XML)
          # TODO: Test Reporten (HTML)